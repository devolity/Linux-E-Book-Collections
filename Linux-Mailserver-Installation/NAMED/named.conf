// generated by named-bootconf.pl

options {
	directory "/var/named";
	/*
	 * If there is a firewall between you and nameservers you want
	 * to talk to, you might need to uncomment the query-source
	 * directive below.  Previous versions of BIND always asked
	 * questions using port 53, but BIND 8.1 uses an unprivileged
	 * port by default.
	 */
	// query-source address * port 53;
};

zone "." IN {
	type master;
	file "db.root";
};

zone "localhost" IN {
	type master;
	file "localhost.zone";
	allow-update { none; };
};

zone "0.0.127.in-addr.arpa" IN {
	type master;
	file "named.local";
	allow-update { none; };
};

zone "organicbrownsugar.com" IN {
	type master;
	file "organicbrownsugar.zone";
	allow-update { none; };
};

zone "1.10.192.in-addr.arpa" IN {
	type master;
	file "organicbrownsugar.com.zone.rev";
	allow-update { none; };
};

zone "blackwood.organicbrownsugar.com" IN {
	type master;
	file "blackwood.organicbrownsugar.com.zone";
	allow-update { none; };
};

zone "100.10.192.in-addr.arpa" IN {
	type master;
	file "blackwood.organicbrownsugar.com.zone.rev";
	allow-update { none; };
};
